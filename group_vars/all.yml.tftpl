---
# Server
server_ip: "${server_ip}"

# GCP
gcp_project_id: "${gcp_project_id}"

# Domain configuration
domain_name: "${domain_name}"
subdomain: "${subdomain}"

# Optional
admin_email: "${admin_email}"
